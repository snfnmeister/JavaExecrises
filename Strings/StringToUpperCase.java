/*
 *
 * Java String.toUpperCase() method. Returns the new low-register string
 * syntax: public String toUpperCase()
 *         public String toUpperCase(Locale locale) - with locale
 */
public class StringToUpperCase {

    public static void main(String[] args) {

        String str1 = "good work my friend!";
        System.out.println(str1.toUpperCase());
    }
}
